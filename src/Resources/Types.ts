const TYPES = {
    // Registry
    MetadataRegistry: Symbol.for('event_dispatching.MetadataRegistry'),

    // Services Declarations
    EventDispatching: Symbol.for('event_dispatching.EventDispatching'),
};

export { TYPES };
